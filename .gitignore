# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
.env

# Data & weights (heavy / not for VCS) — root-anchored so they do NOT match
# the source package src/data/
/data/
/checkpoints/
*.pt
*.pth
*.ckpt

# Runtime artifacts
demo/history.db
*.log
/tmp/
/_unzip/
venya_results.zip
# NOTE: results/ and reports/out/ ARE committed — they are the deliverable
# (metrics, confusion matrices, comparison, report). Only heavy weights (*.pt,
# /checkpoints/) and the dataset (/data/) stay out of VCS.

# OS / IDE / agent
.DS_Store
.idea/
.vscode/
.claude/
